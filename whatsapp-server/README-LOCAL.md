# WhatsApp Server - Локальная разработка

## 🔧 Исправление проблемы с Chromium для Windows

### Проблема
При запуске на Windows возникала ошибка:
```
❌ Failed to launch the browser process! spawn /usr/bin/chromium-browser ENOENT
```

Это происходило потому, что сервер был настроен только для Linux/Docker окружения.

### ✅ Решение

Теперь сервер автоматически определяет операционную систему и настраивается соответственно:

- **Windows/Локальная разработка**: Puppeteer автоматически находит установленный Chrome
- **Linux/Docker/VM**: Использует путь `/usr/bin/chromium-browser`

### 🚀 Запуск для разработки

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Убедитесь, что у вас установлен Google Chrome** (для Windows)

3. **Запустите сервер:**
   ```bash
   npm run dev
   ```

4. **Проверьте логи:**
   Вы должны увидеть:
   ```
   🔧 WhatsApp Client Configuration:
      Platform: win32
      Local Mode: true
      Session Path: C:\path\to\project\.wwebjs_auth
      Chromium Path: Auto-detect
   ```

### 📦 Переменные окружения

Создайте файл `.env` в папке `whatsapp-server/` (по образцу `env.example`):

```env
# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Supabase Configuration (опционально)
SUPABASE_ENABLED=false

# WhatsApp Configuration
WHATSAPP_SESSION_PATH=./.wwebjs_auth
WHATSAPP_SESSION_NAME=local

# Принудительный локальный режим (опционально)
FORCE_LOCAL_MODE=true
```

### 🌐 Деплой на VM Google Cloud

Для деплоя на VM используйте существующие файлы:
- `env.production.vm` - для Google Cloud VM
- `docker-compose.yml` - для Docker контейнеров

### 🔍 Логи и отладка

Сервер теперь показывает подробную информацию о конфигурации:

```bash
🔧 WhatsApp Client Configuration:
   Platform: win32                    # Автоопределение ОС
   Local Mode: true                    # Режим локальной разработки
   Session Path: ./.wwebjs_auth        # Локальная папка сессий
   Chromium Path: Auto-detect          # Автопоиск Chrome
   Node Environment: development
```

### 🎯 Основные улучшения

1. **Автоопределение ОС** - больше не нужно настраивать пути вручную
2. **Умные пути сессий** - локальные папки для разработки, Docker пути для продакшена
3. **Автопоиск браузера** - Puppeteer сам найдет Chrome на Windows
4. **Обратная совместимость** - все настройки для Docker/VM остались неизменными

### 🐛 Решение проблем

**Если сервер не запускается:**

1. Убедитесь, что установлен Google Chrome
2. Проверьте, что порт 3000 свободен
3. Проверьте логи на наличие других ошибок

**Если возникают проблемы с сессией:**

1. Удалите папку `.wwebjs_auth`
2. Перезапустите сервер
3. Отсканируйте QR-код заново

### 📱 Подключение фронтенда

Убедитесь, что в основном проекте `src/` настроен правильный URL для WhatsApp API:

```typescript
const WHATSAPP_API_URL = 'http://localhost:3000';
``` 